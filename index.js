"use strict"

// only change code below this line

var sum = 10 + 0; //change 0 so that the sum is 20

var difference = 15 - 0; //change 0 so that the difference is 5

var product = 8 * 0; //change 0 so that the product is 16

var quotient = 6 / 0; //change 0 so that quotient is 2

// only change code above this line

module.exports = {
    sum,
    difference,
    product,
    quotient
}
